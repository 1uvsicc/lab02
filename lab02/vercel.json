{
    "version": 2,
    "builds": [
      {
        "framework": "nextjs",
        "src": "package.json",
        "use": "@vercel/node",
        "config": {
          "buildCommand": "npm run build",
          "outputDirectory": "dist"
        }
        
      }
    ]
  }